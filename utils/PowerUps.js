const PowerUps = (score) => {
    switch (score) {
        case 5: 
            while (true) {
                score++;
            }
        case 50:
            while (true) {
                score += 5;
            }
    }
};

export default PowerUps;